/**
 * Class is implemented by any and all classes that require the comparison of two objects; in this case, CDBE
 * @author Brandon Tenorio
 */
public interface Comparable {
    int compareTo(CourseDBElement element);
}
